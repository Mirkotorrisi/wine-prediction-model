{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run wine_prediction_model.py (pipenv)",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "env": {
        "PIPENV_ACTIVE": "1"
      },
      "envFile": "${workspaceFolder}/.env",
      "justMyCode": true
    }
  ]
}
